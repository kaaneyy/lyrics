# Copy this file to `.env` and fill in the key(s) for the provider(s) you use.
# The server only exposes /api/chat — keys never reach the browser.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=

# PORT=8765
